<UserControl
    x:Class="SnapIt.Views.AboutView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:c="clr-namespace:SnapIt.Library.Entities;assembly=SnapIt.Library"
    xmlns:vm="clr-namespace:SnapIt.ViewModels"
    d:DataContext="{d:DesignInstance vm:AboutViewModel,
                                     IsDesignTimeCreatable=True}"
    d:DesignHeight="480"
    d:DesignWidth="600"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <d:DesignerProperties.DesignStyle>
        <Style TargetType="UserControl">
            <Setter Property="Background" Value="White" />
        </Style>
    </d:DesignerProperties.DesignStyle>

    <Grid Margin="16,8,8,8">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel HorizontalAlignment="Left">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80" />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>

                    <Image Margin="-1" Source="/SnapIt;component/Themes/snapit.png" />

                    <StackPanel Grid.Column="1" Margin="16,8,0,0">
                        <TextBlock FontSize="18" Text="{x:Static c:Constants.AppName}" />
                        <TextBlock Text="{x:Static c:Constants.AppVersion}" />
                    </StackPanel>
                </Grid>

                <TextBlock
                    Margin="0,24,0,0"
                    Text="Snap Screen is a window manager for Windows. Organizes your windows to improve your productivity when working with wide and multiple screens."
                    TextWrapping="Wrap" />

                <TextBlock>
                    <Hyperlink
                        Command="{Binding HandleLinkClick}"
                        CommandParameter="{x:Static c:Constants.AppUrl}"
                        Style="{StaticResource SnapIt.Styles.Hyperlink}">
                        <Hyperlink.Inlines>
                            <Run Text="{x:Static c:Constants.AppUrl}" />
                        </Hyperlink.Inlines>
                    </Hyperlink>
                </TextBlock>

                <TextBlock
                    Margin="0,24,0,0"
                    FontSize="18"
                    Text="Give Feedback" />
                <TextBlock Text="Help me make SnapIt better. You can add new ideas and features, as well as vote other ideas and submit a bug here." TextWrapping="Wrap" />
                <TextBlock Margin="12,8">
                    <Hyperlink
                        Command="{Binding HandleLinkClick}"
                        CommandParameter="{x:Static c:Constants.AppFeedbackUrl}"
                        Style="{StaticResource SnapIt.Styles.Hyperlink}">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial
                                Width="14"
                                Height="14"
                                Margin="0,0,8,0"
                                Kind="LightbulbOnOutline" />

                            <TextBlock Text="New ideas or report a bug" />
                        </StackPanel>
                    </Hyperlink>
                </TextBlock>
                <TextBlock Margin="12,0">
                    <Hyperlink Command="{Binding RateReviewStoreClick}" Style="{StaticResource SnapIt.Styles.Hyperlink}">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial
                                Width="14"
                                Height="14"
                                Margin="0,0,8,0"
                                VerticalAlignment="Center"
                                Kind="StarOutline" />

                            <TextBlock Text="Rate and review on Microsoft Store" />
                        </StackPanel>
                    </Hyperlink>
                </TextBlock>

                <TextBlock
                    Margin="0,24,0,0"
                    FontSize="18"
                    Text="Follow Me" />

                <StackPanel Orientation="Horizontal">
                    <!--<Button
                        Margin="0,0,12,0"
                        Padding="4"
                        Command="{Binding HandleLinkClick}"
                        CommandParameter="{x:Static c:Constants.TwitterUrl}"
                        Foreground="#FF00ACED"
                        Style="{StaticResource MahApps.Styles.Button.Chromeless}">
                        <Button.Content>
                            <iconPacks:PackIconMaterial
                                Width="24"
                                Height="24"
                                Kind="Twitter" />
                        </Button.Content>
                    </Button>-->
                    <Button
                        Margin="0,0,12,0"
                        Padding="4"
                        Command="{Binding HandleLinkClick}"
                        CommandParameter="{x:Static c:Constants.LinkedinUrl}"
                        Foreground="#FF4875B4"
                        Style="{StaticResource MahApps.Styles.Button.Chromeless}">
                        <Button.Content>
                            <iconPacks:PackIconMaterial
                                Width="24"
                                Height="24"
                                Kind="Linkedin" />
                        </Button.Content>
                    </Button>
                    <Button
                        Margin="0,0,12,0"
                        Padding="4"
                        Command="{Binding HandleLinkClick}"
                        CommandParameter="{x:Static c:Constants.GithubUrl}"
                        Foreground="#7DBBE6 "
                        Style="{StaticResource MahApps.Styles.Button.Chromeless}">
                        <Button.Content>
                            <iconPacks:PackIconMaterial
                                Width="24"
                                Height="24"
                                Kind="Github" />
                        </Button.Content>
                    </Button>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <TextBlock HorizontalAlignment="Left">
                <Hyperlink
                    Command="{Binding HandleLinkClick}"
                    CommandParameter="{x:Static c:Constants.CompanyUrl}"
                    Style="{StaticResource SnapIt.Styles.Hyperlink}">
                    <Hyperlink.Inlines>
                        <Run Text="{x:Static c:Constants.CompanyName}" />
                    </Hyperlink.Inlines>
                </Hyperlink>
            </TextBlock>

            <TextBlock Grid.Column="1" HorizontalAlignment="Right">
                <Hyperlink
                    Command="{Binding HandleLinkClick}"
                    CommandParameter="{x:Static c:Constants.AppPrivacyUrl}"
                    Style="{StaticResource SnapIt.Styles.Hyperlink}">
                    <Hyperlink.Inlines>
                        <Run Text="Privacy policy" />
                    </Hyperlink.Inlines>
                </Hyperlink>
            </TextBlock>
        </Grid>
    </Grid>
</UserControl>